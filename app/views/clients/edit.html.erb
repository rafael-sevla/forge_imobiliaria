<div class="mb-4">
  <h1 class="h3 mb-0">Editar cliente</h1>
  <p class="text-muted mb-0"><%= @client.name %></p>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", client: @client %>
  </div>
</div>
