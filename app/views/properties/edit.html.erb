<div class="mb-4">
  <h1 class="h3 mb-0">Editar imóvel</h1>
  <p class="text-muted mb-0"><%= @property.title %></p>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", property: @property %>
  </div>
</div>
